# File: .env.example

GIN_MODE=debug
DATABASE_URL=postgres://admin:secret@localhost:5432/asset_db?sslmode=disable
JWT_SECRET_KEY=jwtsecretpassword

# LDAP bind_password encryption key — AES-256, tepat 32 byte atau base64 dari 32 byte
# Generate: openssl rand -base64 32
LDAP_ENCRYPTION_KEY=